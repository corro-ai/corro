{
  "name": "corro",
  "version": "0.1.0",
  "private": true,
  "description": "The open-source evidence layer for spec-driven development.",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "packageManager": "pnpm@9.1.0",
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.4",
    "dotenv": "^17.4.2",
    "tsx": "^4.19.0"
  }
}
